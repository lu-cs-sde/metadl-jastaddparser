Row and column numbers are calculated correctly when JastAddParser is invoked with --no-beaver-symbol, optional non-terminals are present in the grammar and input matches a production where an optional element is not matched.